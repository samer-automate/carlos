services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - carlos_data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/carlos.db
    restart: unless-stopped

volumes:
  carlos_data:
